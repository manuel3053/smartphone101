#import "../template-alfianellux/alfianellux.typ" : *
#import "../template-alfianellux/colors.typ" : colors
#import "../template-alfianellux/slides.typ" : *

#title(title: "RUBRICA")

#horizontal-halved-split(
  top: [
    #image("img/contacts.svg")
  ],
  bottom: [
    - effettuare chiamata
    - gestire contatto
    - bloccare contatto
  ]
)

#title(title: "WHATSAPP")

#horizontal-halved-split(
  top: [
    #image("img/whatsapp.svg")
  ],
  bottom: [
    - effettuare chiamate
    - vedere stati e canali
    - scrivere in gruppo o chat
    - backup chat
  ]
)

#title(title: "BROWSER")

#horizontal-halved-split(
  top: [
    #image("img/firefox.svg")
  ],
  bottom: [
    - ricerca
    - gestione schede
    - download
    - preferiti
  ]
)


#title(title: "FACEBOOK")

#horizontal-halved-split(
  top: [
    #image("img/facebook.svg")
  ],
  bottom: [
  ]
)


#title(title: "INSTAGRAM")

#horizontal-halved-split(
  top: [
    #image("img/instagram.svg")
  ],
  bottom: [
  ]
)


#title(title: "GMAIL")

#horizontal-halved-split(
  top: [
    #image("img/gmail.svg")
  ],
  bottom: [
    - visualizzare mail
    - creare mail
    - caricare e scaricare allegati
  ]
)

#title(title: "PLAYSTORE")

#horizontal-halved-split(
  top: [
    #image("img/google-play.svg")
  ],
  bottom: [
    - installare app
    - aggiornare app
    - disintallare app
  ]
)

#title(title: "MENÙ A TENDINA")

#horizontal-halved-split(
  top: [
    #image("img/dropdown.svg")
  ],
  bottom: [
    #columns[
      - suoneria
      - bluetooth
      - wifi
      #colbreak()
      - modalità aereo
      - torcia
    ]
  ]
)

#title(title: "GOOGLE MAPS")

#horizontal-halved-split(
  top: [
    #image("img/google-maps.svg")
  ],
  bottom: [
    - ricerca località
    - selezione metodo di viaggio
    - selezione percorso
  ]
)

#title(title: "GOOGLE DRIVE")

#horizontal-halved-split(
  top: [
    #image("img/google-drive.svg")
  ],
  bottom: [
    - navigazione
    - scaricare file
    - caricare file (?)
  ]
)

